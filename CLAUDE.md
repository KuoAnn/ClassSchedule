# Claude 入口

這個檔案僅作轉址用途。

本專案的實際規格由 **[AGENTS.md](AGENTS.md)** 管理，
Claude 接手前請先讀 **[AGENTS.md](AGENTS.md)**，再動任何一行程式。

> 版面拆在 `src/`（HTML／CSS／JS／SVG），打包時由 `scripts/build.py` 內嵌成單一 HTML。
> 動 `src/` 之前請先看 AGENTS.md 的「拆檔後的四條約束」。
