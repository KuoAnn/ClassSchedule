# Agent 指示

這份檔案是本專案的**唯一規格來源**。

本專案保留 **[CLAUDE.md](CLAUDE.md)** 與 **[AGENTS.md](AGENTS.md)** 作為 AI 入口檔，
但**實際工作規格由本檔管理**。任何 AI 助理接手前，請先讀完本檔，再動任何一行程式。

## 專案形狀

```text
src/                  版面範本資源，可個別重用（打包時內嵌成單一 HTML）
  index.html          外框；{{title}} {{og_desc}} {{styles}} {{boot}} {{content}} {{scripts}}
  styles/01..13*.css  樣式，數字前綴＝串接順序
  js/01..09*.js       腳本，數字前綴＝串接順序
  icons/*.svg         內嵌圖示（`favicon.svg` 走 data URI，見下）
scripts/spec.py       匯入格式規格表：欄位、分類色票、師資標註、級別、課名對照
scripts/build.py      產生器：CSV → 卡片 HTML，再把 src/ 內嵌成單一檔
scripts/shots.py      下載圖：用 playwright 把課表畫成四張 PNG（寬／窄 × 中／英）
scripts/run.sh        產檔 + 產下載圖 + 四項檢查
scripts/checks/       occl.py／wcag.py／catcheck.py（＋ clock.py、de.py 為輔助）
data/                 課程 CSV、template.csv、csv-prompt.md（丟給 AI 產 CSV 的提示詞）
dist/                 產物：單一 HTML ＋ 四張 PNG
.github/workflows/    推 main 時產檔、跑檢查、發佈 Pages
```

## 核心規則

1. 只改 `src/` 與 `scripts/`，不要改 `dist/` 裡的 HTML（那是產物）
2. 改完必跑：`./scripts/run.sh`，四項檢查**全部必須是 0**（不是 0 會直接以非 0 結束）
3. 不要為了讓檢查通過而放寬標準
4. 新增規格時，把「為什麼」一起寫進本檔
5. 若要為 AI 入口檔做跨檔對齊，請維持 `AGENTS.md` 為主，`CLAUDE.md` 做轉址

## 拆檔後的四條約束

拆成資源檔是為了好維護，但打包後必須與原本的單檔行為一致，所以：

1. **檔名數字前綴就是串接順序，不要重排、不要改名而不改順序。**
   CSS 有幾組 specificity 相同、靠先後決勝負的規則，例如
   `.sheet.flat .ndock{display:none}` 與 `.ndock.show{display:flex}`、
   `.sheet.noto .tdy{display:none}` 與 `.dn.today .tdy{display:inline-block}`。
   `05-export-mode.css` 因此必須夾在 `04-narrow.css` 與 `06-narrow-card.css` 之間，
   搬到最後會改變匯出圖片的外觀。
2. **JS 是串成同一個 `<script>` 的**，彼此靠全域 `var`／`function` 互相呼叫
   （`fitCells`、`markPast`、`track`、`syncRows`、`picked`…）。
   拆檔時不要加 `"use strict"`、不要包成 IIFE、不要改成 module，會直接壞掉。
   `01-boot.js` 例外：它單獨放在 `<body>` 開頭，必須在畫面繪製前決定 `html.nv/.wv`。
3. **build 期的值不寫死在資源檔裡。**
   `01-tokens.css` 的 `--gy`／`--gut`／`--sheetw` 只是離線預覽的預設值，
   `build.py` 會在樣式尾端補一段 `:root{…}` 覆寫；
   PNG 檔名走 `window.SCHEDULE`（由 build 產生，`08-export.js` 沒有時退回 `schedule.png`）。
4. **`src/index.html` 的 `{{…}}` 佔位符要與 `build.py` 的 `FILL` 一致**，
   多打少打都會在 build 時直接丟 `KeyError`（這是刻意的，不要改成靜默略過）。

## 匯入格式：一列一堂課，未定義的值不擋產檔

CSV 固定 12 欄，順序不可調換（缺欄位 `build.py` 直接停，多的忽略並 WARN）：

```text
星期,開始時間,結束時間,課程名稱,師資名稱,師資國籍,教學語系,分類,級別,異動類型,異動老師,異動日期
```

**可擴充的定義是「沒定義過的值也產得出來」**：新分類自動配一個色
（`spec.auto_color()`，由名字雜湊出色相，明度彩度釘在既有色票的水位），
新的國籍／語系／級別直接沿用原文。**判斷不出來的一律只印前兩個字**
（`spec.short_tag()`）：標籤的空間很小（窄版卡片 142px），整串印會撐爆或被裁掉，
兩個字足以辨識，全名仍留在 aria-label 與色票列的說明裡。都會印 WARN 要人補譯名與色票 ——
讓這個月的課表先出得去，而不是讓它悄悄出錯。對照表全部集中在 `scripts/spec.py`，
`build.py` 產檔與 `checks/catcheck.py` 驗色**讀同一份**（catcheck 以前自己抄一份
色票，加分類時很容易只改到一邊，檢查就變成在驗舊資料）。

- **師資拆成名稱／國籍／語系三欄。** 舊格式是 `JAI（印籍）（EN）` 一格塞到底，
  程式得用正規表示式把括號挖出來，而且同一位老師常常有幾列漏標 ——
  舊版是靠 `ALWAYS_EN = {"JAI", "Roushan"}` 這種白名單硬補回來的。拆開之後
  標註就是資料本身，白名單刪掉了，改由「同一位老師在不同列標註不一致」的 WARN 把關。
  國籍固定一個字（`印`＝印度），未設定就整顆標籤不出現。
- **異動也是三欄，多筆以半形分號分隔、依位置配成一組**
  （`代課;暫停` ＋ `Jen;` ＋ `9/7;9/28` ＝ 9/7 Jen 代課、9/28 暫停）。
  舊格式把兩種異動併成 `代課／暫停` 一格，日期則靠「第一個是代課、其餘是暫停」
  這種位置慣例去猜；多一種異動類型就得全部重寫。分號切開後三欄的**段數要一致**，
  `暫停` 那一段的異動老師留空但分號不能省，否則整排錯位（會 WARN）。
  `暫停` 填了異動老師一律以「暫停」處理並 WARN —— 這條沿用舊規則。
- **級別未設定就不顯示；設定成「預設級別」也不上標籤。**
  `spec.LEVELS` 裡 `tag=None` 的（目前是 `初`）只進 aria-label、不印在卡片上，
  跟「一小時的課只印起始時間」同一條理由：預設值印出來只是雜訊，
  而且這份課表 107 堂裡有 92 堂是初級，全印會把窄版 142px 的卡片擠爆。
  要讓 `初` 也顯示，就在 `spec.LEVELS["初"]` 補一組 `tag` 與 `style`。
- **教室欄位移除。** 從來沒有被讀過，留著只會讓每個月填表的人以為要填。

格式遷移（欄位改了但課程沒改）**不是改版**，所以 `data/versions.json` 要把該月的
舊指紋換成新的，而不是 append —— 版本號代表「這份課表改到第幾版」，
排版與格式的變動不該讓學員以為課表改了。

`data/csv-prompt.md` 是把欄位規格寫成可以直接貼給 AI 的提示詞，
改欄位時它與 `spec.COLUMNS`、`data/template.csv` 三份要一起改。

## 站台圖示（favicon）

`src/icons/favicon.svg`（K 標誌）由 `build.py` 的 `data_icon()` 轉成 data URI，
填進 `src/index.html` 的 `{{favicon}}`。**不放外部 `.ico`／`.png`**：
HTML 本身要維持「一個檔就能看」—— 有人只把那份 HTML 存下來寄出去，外部檔就會 404。
（`dist/` 裡另外有四張下載圖，那是按下載才導過去的獨立檔案，不是 HTML 的相依。）
圖示是純幾何 SVG、沒有 `<text>`，才不會在缺字型的環境裡變成空白。

## LINE LIFF：五條約束

站台掛 GitHub Pages，實際由 LINE LIFF 開啟，所以「手機 WebView」才是主場景。
**本站是純靜態課表、不讀任何會員資料**，因此不呼叫 `liff.login()` / `liff.getProfile()`，
也不設 `withLoginOnExternalBrowser` — 一般瀏覽器開同一個網址不會被導去登入。

1. **viewport 只有一份，寬版不縮頁：原尺寸＋橫向捲動。**
   `src/index.html` 從頭到尾都是 `width=device-width,initial-scale=1,viewport-fit=cover`，
   兩種版型共用，JS 不再動 `<meta>`。寬版仍然是 `--sheetw`（2040px）寬的海報，
   螢幕塞不下就讓整份文件橫向捲動，字級維持原尺寸。
   **這是刻意換掉「整頁縮到裝置寬」的**（`setViewport()`／`DEVW`／`remeasureViewport()`
   連同 `01-boot.js` 裡那段算 `initial-scale` 的程式都刪了），因為那條路有兩個致命傷：
   - **縮完根本讀不到。** 2040px 的海報縮到 390px 裝置寬是 0.19 倍，
     16px 的課名只剩 3px、10.5px 的標籤剩 2px。「一眼看完整張」換來的是一張看不懂的圖。
   - **viewport 變成 2040 之後所有 RWD 都失效。** `@media (max-width:900px/560px)`
     跟 `13-liff.css` 的 `(max-width:430px)` 量的是 viewport 寬，不是螢幕寬 ——
     手機上寬版的標題列、按鈕、色票列全部照桌機尺寸出，等於寬版整個沒有 RWD。
     這也是原本的回報：「寬版模式下需 RWD」。
   想看全貌的人雙指縮小就是以前的樣子 —— 沒有設 `maximum-scale`／`user-scalable=no`，
   **也不要補**：那會把「縮出去看全貌」與「放大看細節」同時擋掉。
   - 一進寬版停在最左邊等於停在星期一，所以 `showToday()`（`05-view.js`）在
     **真的有橫向溢出時**才把 `.col.today` 那一欄捲進畫面（桌機放得下整張就不動），
     而且只改水平位置：垂直位置是使用者當下在看的地方。
   - 寬版的卡片是 `left/width` 用 % 算的、`.wrap` 寬度綁 `--sheetw`，所以
     **不管裝置多寬，卡片幾何都一樣** —— `occl.py` 在 2100px 量到的結果仍然代表手機。
     反過來說，不要為了手機把 `--sheetw` 改小：那會讓欄寬縮水、卡片重新換行，
     而檢查是在 2100px 跑的，抓不到那種溢出。
2. **`html` 的 class 只能用 `classList` 動，不可以直接指派 `className`。**
   `01-boot.js` 在繪製前掛上的 `.liff`（UA 含 `" Line/"`）會被整串覆蓋掉，
   `inLINE()` 就會回 false，「把圖丟給外部瀏覽器」那條路就失效。這個坑踩過一次。
3. **在 LINE 裡存不了前端現畫的圖，所以圖改成 build 時就畫好。**
   `<a download>` 與 blob 在 LINE 的 WebView（iOS 尤其）直接沒反應。
   現在 `scripts/shots.py` 在產檔之後畫好四張 PNG 放在 HTML 旁邊，
   按下載＝導到那張圖的固定網址（`08-export.js` 的 `shotUrl()`），
   瀏覽器直接顯示圖片，長按就能存到相簿。
   - 在 LIFF 裡仍要丟出去給外部瀏覽器（`openExternalDownload(url)`，`09-liff.js`）：
     LINE 的 WebView 顯示圖片時沒有「儲存」。只有 `liff.isInClient()` 為真才算 LIFF；
     從聊天室點一般連結進來的是 LINE 內建瀏覽器，那裡 `openWindow` 只會在 LINE
     自己開新分頁，等於沒解決問題 —— 那條路就退回 `window.open`，一樣看得到圖。
   - **`?dl=1&v=…&l=…&cat=…` 那條路整段沒了。** 以前 PNG 是前端現畫的、沒有網址可以傳，
     只好用參數把整份課表在外部瀏覽器重現一次再畫。現在圖本身就有網址。
     `saveImage()` 的長按覆蓋層與 `.shot` 樣式也一起拿掉了。
   - `qparam()` 仍留在 `01-boot.js`：`?v=n` 這種分享連結還在用它，
     而且 `v` 必須在繪製前就讀到。網址帶 `v` 時 `05-view.js` **不寫** `localStorage` ——
     別人分享的連結不該蓋掉這台瀏覽器自己記住的版型。
4. **手機的 canvas 上限比桌機低。**
   `08-export.js` 只在 LINE 或視窗 < 900px 時把 `scale` 壓到 12M 畫素以內；
   桌機維持 `scale:2`，匯出結果與過去完全相同。
5. **`13-liff.css` 放在最後是刻意的。**
   裡面都是要蓋過前面版型的補丁（現在只剩安全區內距）。
   原本還有一組 `html.nv` 的手機字級覆寫擺在這裡 —— 因為 `html.nv .hd h1` 這種
   選擇器的 specificity 比 `@media` 裡的 `.hd h1` 高，寬版縮、窄版不縮，
   只好在最後再補一份。標題列改成共用元件之後（見下一節）那組覆寫刪掉了，
   `(max-width:430px)` 併回 `07-header.css`，兩種版型讀同一組變數。

`LIFF_ID` 由 `build.py --liff-id`（或環境變數 `LIFF_ID`，CI 走 repo Variable）帶入，
產生 `window.LIFF_ID`；沒設也不會壞，只是不初始化 SDK。設定步驟見 README。

## 標題列：一份元件、兩種版型共用

標題、功能鈕、色票篩選這三列在 HTML 裡本來就只有一份（`.top` 裡的 `.hd`／`.tools`／`.keys`，
`build.py` 只產一次），但**尺寸有四份**：寬版一組（`.hd h1` 34px，`07-header.css`）、
窄版一組（`html.nv .hd h1` 26px，`03-view-switch.css`）、`@media` 900／560 再縮一次、
`13-liff.css` 的 `(max-width:430px)` 又補一次。同一顆按鈕的內距散在四個檔案裡，
靠 specificity 與串接順序決勝負 —— 改一處要回頭檢查另外三處，而且踩過一次：
基準尺寸原本擺在 `07-header.css` 檔尾，把 `@media` 的縮小值整組蓋了回去。

現在**寬版直接沿用窄版那組尺寸**，四份覆寫收成一份：

- 尺寸全部是 `.top` 上的變數（`--h1`／`--h1sub`／`--by`／`--btnH`／`--segpad`／
  `--icon`／`--keyfs`／`--tpx`…），規則本身只讀變數，**斷點只重新宣告變數**。
  覆寫都是同一個選擇器（`.top`）在同一個檔案裡，順序與 specificity 不再是變因。
- 變數宣告在 `.top` 而不是 `:root`：這三列都在 `.top` 裡面，靠繼承拿值就好，
  而 `:root` 那份會被 `build.py` 在樣式尾端覆寫（見「拆檔後的四條約束」第 3 條）。
- **不要再用 `html.nv`／`html.wv` 去覆寫標題列尺寸。** 那正是被收掉的東西；
  要改就改變數，兩種版型一起變。版型差異只剩 `03-view-switch.css` 裡的
  外框寬度、`.sheet` 內距，以及 `wideonly`／`narrowonly`。
- 色票列改成**兩種版型都單列橫向捲動**（以前只有窄版是，寬版 `flex-wrap:wrap`）。
  換行會把標題列多撐一整行，而 `--dockH` 一高，釘住的星期列與 `.topspacer`
  就整個往下推 —— 寬版在筆電寬度剛好會換行，等於課表被固定工具列吃掉一行。
  出血寬度綁 `--tpx`（`.top` 的左右內距），捲到底剛好貼齊版面邊緣，
  斷點改內距時不必回頭改 `10-legend.css`。
- 色票列一單列捲動，排在最後面的「顯示全部」（`.clear`）就會被捲出可視範圍，
  等於篩選解不掉。所以篩選開著時（`.keys.on`）把它 `position:sticky;right:0` 釘在右緣，
  同時**把出血收掉**（`.keys.on{margin-right:0;padding-right:0}`）—— sticky 的 `right`
  是對捲動容器的 padding box 量的，不收出血的話按鈕右邊會露出一段還在捲的色票。
  按鈕的 `box-shadow` 是同色外框（蓋掉從底下捲過去的色票），不是陰影。
- 匯出的圖裡沒有人可以捲，所以 `05-export-mode.css` 用 `.sheet.flat .keys`
  把色票列改回換行、出血收掉。**掛在 `.flat` 上**（不是 `.weekexp`）是因為
  寬窄兩種圖都有 `flat`、只有窄版有 `weekexp` —— 寬版的色票列以前靠基準值就是換行的，
  基準改成不換行之後就得補這一條，否則寬版的圖會把後面幾個分類裁掉。

實測（`.top` 的高度＝`measureDock()` 量出來的 `--dockH`，改尺寸不必手動調）：

| 視窗寬 | 寬版（改前 → 改後） | 窄版 |
| --- | --- | --- |
| 1440px | 130px → 124px | 124px（沒動） |
| 900px | 105px → 113px | 113px（沒動） |
| 390px | 101px → 105px | 105px（沒動） |

窄版的數字一格都沒變 —— 這次改的全是「把寬版拉來對齊窄版」。
連帶地，**寬版下載圖的標題也跟著縮到 26px**（`shots.py` 是把視窗調成版面寬再截圖，
走的就是基準那組變數）。想讓 2040px 的海報標題大一點，正確做法是加一條
`html.wv .sheet.flat{--h1:…}` 只改變數，不要把整組規則抄回去。

## 寬版的兩條 sticky 軸：時間優先

寬版有兩條 sticky：星期列 `.headrow`（`top:var(--dockH)`）與時間欄 `.gut`（`left:0`）。
兩條軸的交會處只能有一個贏，輸的那一邊會把「剛好停在邊界上的那一格」切一半。

**時間欄贏**（`.gut` 的 `z-index:26` > `.headrow` 的 `25`）。理由是不對稱的：
時間欄只有 `--gut` 寬，蓋過去的位置是 `.hcorner` 那塊空白角落，
**一個星期字樣都不會被遮到**；反過來星期蓋時間，是實實在在少掉一行時間。
代價只有一個：星期列的底線在最左邊那 56px 不畫 —— 那一段本來就是空白的角落，
不算資訊（捲動時時間欄的底色本來也會蓋掉它）。

**所以 `.hcorner` 沒有 `border-bottom`，不要「補回來」。**
時間欄的第一個標籤（`09:00` 與早／午／晚）是 `transform:translateY(-50%)` 對齊格線的，
而第一條格線就是 `.gut` 的上緣＝角落那格的下緣，底線會正好從第一個時間的字中間
穿過去（看起來像時間被切一半）。星期那七格的底線由 `.dn` 自己畫，仍然是連續的。

改動 z-index 前先看一眼這串：`.top` 30 ＞ `.nav` 28 ＞ **`.gut` 26 ＞ `.headrow` 25**
＞ `.ndock` 24。`.hcorner` 的 `z-index:3` 是 `.headrow` 內部的區域值
（`.headrow` 有 z-index、自己開了一個 stacking context），不要拿來跟上面這串比。

## 卡片上的時間：一小時只印起始時間

一小時是這裡的預設時長（115 年 9 月這份 107 堂裡有 92 堂），
結束時間等於「起始 + 1 小時」、印出來是多餘的資訊，所以 60 分鐘的課只印 `HH:mm`
（`09:00`）。其他時長照樣印起訖並附上分鐘數（`10:00–11:15 (75)`），
那顆 `(75)`／`(90)` 就是「這堂不是一小時」的訊號。

- 這條取代了舊的「起訖皆整點就省略分鐘」（`11:00–12:00` → `11–12`）。
  舊規則實際上只有 60 分鐘的課碰得到 —— 這份課表的時長只有 60／75／90，
  沒有任何非 60 分鐘的課是整點起訖，所以直接換掉，不必兩套規則並存。
- **無障礙不受影響**：`labz`／`labe`（`aria-label`）的起訖時間是另外組的，
  一直都是完整的 `09:00 至 10:00`，不要為了「一致」把它也砍成起始時間。

## 課表版本：只跟 CSV 一起跑

課表會在月中改（代課、暫停），改過就要重新貼進 LINE，所以圖上要有版本號讓人分辨新舊。
規則：**一個月從 1.0 開始跑，1.0 不印出來**（當月第一版不需要標「第一版」），
1.1 起才在署名旁邊以小字顯示 `v1.1`。

版本代表「這份課表改到第幾版」，不是站台版本，所以 `resolve_version()`（`scripts/build.py`）
**只看 CSV 內容**：改版面、改樣式、重新產檔都不該讓版本跳號。編號記在 `data/versions.json`，
一個月一筆（key 是 `<館別>-<年>-<月>`），值是這個月依序出現過的 CSV 指紋，
指紋在陣列裡的序號就是小版號。

- 指紋算之前會把 CRLF 與尾端空白正規化 —— 編輯器換個存檔格式不該算改版。
- 沒見過的指紋會 append 並寫回 json。**這個 json 要跟著 commit**：
  CI 端只要 json 是最新的，算出來就跟本機一致；寫不進去（唯讀環境）也不會壞，
  版本仍然算得出來，只是下一次沒有紀錄可對。
- 要手動指定用 `--version 1.3`（`--version 1.0` 一樣不顯示）。
- **`resolve_version()` 要在 CSV 驗過欄位之後才呼叫。** 它會把沒見過的指紋寫回
  `versions.json`，放在驗證之前的話，連「欄位不對、根本產不出檔」的那一次也會
  佔掉一個版本號（曾經在 `versions.json` 留下一筆根本不存在的館別）。
- **只換 CSV 的欄位格式不算改版。** 指紋是算原始文字的，所以搬格式時要自己把
  `versions.json` 裡那一筆換成新檔的指紋（位置不變），課表內容沒動就還是同一版。
- `.ver` 是 11px，**不要加 `opacity`**：`--ink3` 本身在 `--page` 上是過的，
  乘上 .85 就掉到 3.66:1，而小字要 4.5:1，`wcag.py` 會直接抓出來。
  這條到 1.1 才會被測到 —— 1.0 根本不印那顆。
- **署名 `.by` 一律顯示，手機也不藏。** `07-header.css` 原本在
  `@media (max-width:560px)` 把它 `display:none`（純粹為了省標題列高度），
  但署名與版本號是課表的落款，手機看的人跟匯出的圖都不該少這行。
  拿掉之後標題列在 375px 高 105px（多一行），`--dockH` 是 `measureDock()`
  量出來的，釘住的星期列與 `.topspacer` 會自己跟上，不必手動調。

## 窄版單日寬度：算出來的，不是寫死的

窄版原本一天固定 320px，手機一次只看得到一天。但單日寬其實只要「卡片還讀得下去」就夠，
剩下的寬度拿去多擺一天更有價值 —— 手機轉橫、平板、大螢幕手機都塞得下兩三天，
可以直接橫向比較。所以 `sizeDays()`（`04-narrow.js`）依可視寬算：

```text
W   = track.clientWidth - gap                 可用寬（讓回一個 gap，理由見下）
per = floor((W + gap) / (MINDAY + gap))       能擺幾天（上限 7）
--dayw = min(MAXDAY, floor((W - (per-1)*gap) / per))    平分剩下的寬度
```

- `MINDAY = 142`：實測的下限 —— 時段、老師、`9/5・9/26 暫停` 這種標籤都還放得進一行，
  107 張卡在 320～1024px 全部沒有水平溢出。這個值也決定了「幾寬才看得到兩天」：
  定 150 時 375px 的手機只剩一天，142 才剛好兩天。
  `MAXDAY = 320` 是原本的單日寬，螢幕再大也不要把卡片拉更寬（拉寬只是讓一行字更空）。
- **要讓回一個 `gap` 的寬度**，缺了看起來就是「卡片被切掉」：軌道是出血到螢幕邊的
  （`.nlist` 的 `margin-right:-14px`，原本是為了「剛好貼到可視邊緣」），不留白的話
  最後一欄的右框與圓角正好壓在邊界上；而讓回的剛好是一個 `gap`，下一欄的左緣
  就正好落在裁切邊上、露不出來。
- **非 full 一律置中吸附**：不論一次看得到幾天，都維持 `scroll-snap-align:center`，
  進入窄版先把今天置中，按翻頁與拖拉放手都定位到新的置中星期。這樣其餘星期會從中心
  往兩側平均露出，定位點語意也一致，不會有「某些寬度靠左吸附、某些寬度置中」的跳變。
- 邊緣淡出改成左右對稱：只在最外側那天被裁切時才出現，寬度就是「半張露出的實際可見量」，
  看起來是邊界柔化而不是卡片被切掉。
- **`syncRows()` 一定要排在 `layout()` 後面**（`04-narrow.js` 的 resize 與初始化、
  `05-view.js` 切到窄版）：`layout()` 會算出新的 `--dayw`，卡片寬度變了換行就變了，
  先量高度等於拿舊寬度的結果去對齊。
- **翻頁鈕用 `clientWidth` 定位，不要用 `innerWidth`**（`placeNav()`）：
  `innerWidth` 含垂直捲軸的寬度，桌機把窄版視窗縮小時右邊那顆鈕會被推到捲軸底下，
  看起來就是被切掉。

## 釘住的星期列：要跟輪播對齊

`.ndock` 取代的是各日面板的標頭（釘住時 `.ntrack.pinned .dhd` 被藏起來），
所以它的幾何必須跟輪播一模一樣，否則釘住的瞬間整排星期會跳位：

- 每一格 `--dayw`、間距 `--gap`，整條 `.ndockin` 再用 `translateX(-scrollLeft)`
  跟著輪播走（窄版已經沒有左側時間軸，所以不需要再讓開一段寬度）。
- **一格要對一個輪播面板（21 格，含前後兩組複製），不是只對七個真實日子。**
  只做七格的話，慣性滑進複製區時 dock 會整排跑掉（要等 `normalize()` 才歸位）。
- 裁切放在 `.ndockclip` 上，不是 `.ndock` 上：`overflow` 是裁到 padding box，
  裁不掉自己的 `padding`，標籤會滑到時間欄上面。`.ndock` 自己要塗滿背景（含時間欄
  那一段），否則下面的卡片會透出來。
- 早期版本是「七天平分整個寬度」的分頁列。一次只看得到一天時看不出問題，
  但改成一次看得到兩三天之後，星期就會落在別的日子的欄位上。不要改回去。

## 窄版：一天一串，全緊密排列

窄版沒有左側時間軸，也沒有任何跨日對齊：每一天就是自己的一串卡片，連續往下排。
這條走過三個版本，前兩個都留在這裡當紀錄，**不要再走回去**：

| 版本 | 週一那一欄 | 問題 |
| --- | --- | --- |
| 逐時對齊（`.hrow[data-h]`）＋時間欄 | 2907px | 空堂處大片留白，一列的高度由「該小時最滿的那一天」決定 |
| 早／午／晚三段對齊＋只標段的時間軸 | 2299px | 留白變小但還在，段內各日堂數不同還是要對齊 |
| **全緊密排列、沒有時間軸** | **1793px** | — |

會有留白就是因為有共用座標要對齊。**每張卡自己都印著時段**，一天一串往下滑
本來就不需要共用座標，所以最後連時間軸一起收掉 —— 少的只有早／午／晚的分段圖示。

- `syncRows()` 只剩一件事：各日的標頭等高（並排的，不齊的話第一張卡會從不同高度開始）。
  名字留著不改，`resize`、切版型、切語言三處都在呼叫它。
- `.hrow[data-b]` 留著只當早／午／晚的分組容器（`NBANDS`），沒有邊線也沒有固定高度；
  空的整段 `display:none` 收掉，卡片就連續。
- 收掉時間欄多出 48px 給日欄：390px 的手機從 152px 變成 176px（仍是兩天）。

### 卡片內的換行規則：區塊換行、標籤裡的字不換行

卡片壓窄（左右內距 8px、匯出的 `--dayw` 148px）之後，右上角那塊空白不見了，
但也換來「標籤排不下」的問題。處理方式一律是**整顆標籤換到下一行，標籤裡的字不換行**
（`.lc .lv`、`.lc .x>span`、`.lc .cg` 都是 `white-space:nowrap`）：

- **級別標籤**（`.lc .nr`）：`flex-wrap:wrap` ＋ `.lv{margin-left:auto}`。
  排得下就照舊靠在課名右邊，排不下整顆掉到下一行、仍然靠右。
  英文課名有 `Fundamental` 這種斷不開的長單字，逼它跟標籤共用一行會直接壓上去。
- **異動標籤與分類標籤**（`.lc .bt`）：`.lc .x{display:contents}` 把 `.x` 攤平，
  讓兩種標籤排在同一個換行流裡。`.x` 若維持 flex item，它會先縮到「最寬的那一顆」
  再自己內部換行，分類標籤就被留在第一行右邊，變成上面兩行標籤、下面一整行只有分類標籤：

  ```text
  9/25 Michelle 代
                放鬆
  ```

  只在 `.lc` 攤平，`.ev`（寬版）維持原樣 —— `occl.py` 量的是 `.ev` 底下的
  `.x` 方框，`display:contents` 會讓它量到 0×0。

## 下載圖片：build 時預製四張

`scripts/shots.py` 在產檔之後用 playwright 把課表畫成四張 PNG
（寬／窄 × 中／英，檔名由 `build.py` 寫進 `window.SCHEDULE.png`），放在 HTML 旁邊。
按下載＝導到那張圖的網址，瀏覽器直接顯示圖片。

為什麼不再用 html2canvas 前端現畫：

- 手機畫這張要好幾秒，而且 canvas 有畫素上限，窄版整週那張在舊機器上會直接失敗
  （舊版還得依視窗大小把 scale 壓到 12M 畫素以內）。
- LINE 的 WebView 存不了 blob／`<a download>`，以前得用 `?dl=1&v=…&l=…`
  把整份課表重現到外部瀏覽器再畫一次 —— 一條純粹為了「畫圖」而存在的長路。
- 預製之後圖有固定網址，LINE 裡長按即可存到相簿，前端也不用再載 html2canvas。

**圖檔名不帶月份，網址是固定的。**
`古亭館-課表.png`／`古亭館-課表-直式.png`／`Guting-Studio-schedule.png`／
`Guting-Studio-schedule-vertical.png` —— 站上永遠只有當月這一份課表，
每次部署就地覆蓋同名檔案。理由是這四個網址會被貼到別的地方長期使用
（LINE 圖文選單、官網、書籤），檔名帶月份等於每個月換一組網址、每個月要重設一次。
月份印在圖裡，不需要也不該進檔名。

- 代價是同一個網址的內容會換，而 Pages 是 `Cache-Control: max-age=600`（附 ETag）——
  換月之後那十分鐘可能拿到上個月的圖。所以**下載鈕的網址會多帶一個 `?s=<月>-<版本>`**
  （`window.SCHEDULE.stamp`，`08-export.js` 的 `shotUrl()` 加的）把快取破掉；
  分享用的固定網址不帶，過幾分鐘自然更新。
- 改檔名規則要同時想到：舊網址會 404（沒有轉址），貼出去的連結會全部失效。

幾條要一起維護的約束：

- **`shots.py` 的 `EXPORT_JS` 與畫面的匯出狀態是同一組 class**（`nopast`／`flat`／
  `noto`／`npack`／`weekexp`），改一邊要想到另一邊。它是冪等的，重複套用沒有副作用。
- **視窗寬度要先調成版面寬再截圖**，否則 `@media` 會照「手機視窗」出小一號的標題列。
  改視窗會觸發 `resize`（重算 `--dayw`、重排），所以調完要再跑一次 `EXPORT_JS` 收尾。
- **圖的尺寸不能跟著裝置跑**：`.sheet.weekexp` 把 `--dayw` 釘在 `148px`
  （跟手機上算出來的 143～176px 差不多，所以卡片比例與換行都跟畫面一致）。
  宣告要放在 `.sheet` 上才蓋得過 `sizeDays()` 寫在 `:root` 的 inline 值 ——
  自訂屬性是靠繼承解析的，`.dgrp` 會先看到 `.sheet` 這一層。
  這個值的下限是**最寬的一顆異動標籤**（英文 `9/5 Ding-Ding Sub` 約 115px）
  加左右內距，因為標籤裡的字不換行；再窄就會壓出去。
- **色票列不可以決定整份圖的寬度**：`.sheet` 是 `width:max-content`，
  色票列排成一長條時它就是最寬的那個小孩，七欄縮窄後右邊會留下一塊空白
  （英文版曾多出 240px）。`.sheet.weekexp .keys` 用 `width:0;min-width:100%`
  退出 `max-content` 的計算，再撐回容器寬、排不下就換行。
- **下載的是「完整課表」，不是「畫面快照」**：`EXPORT_JS` 先把分類篩選解掉
  （留著的話沒中的課會被壓到 `opacity:.14`，變成一張半透明的殘缺課表），
  再拿掉「現在幾點、今天星期幾」（`nopast`＋`noto`，並移除 `.stt` 與
  `.st-done`／`.st-live`）。圖要跟「什麼時候按下下載」無關。
- **CI 要把 PNG 一起帶走**：`upload-artifact` 收 `dist/*.html` ＋ `dist/*.png`，
  少了 PNG，Pages 上按下載就是 404。
- `npack` 只壓卡片密度（內距、行距、`.bt` 上緣間距，空的 `.bt` 收掉）——
  螢幕上那些留白是為了好點擊，印成圖只是白費高度。版面本身與畫面完全一樣。

## 參考資訊

- **規格入口**：本檔 `AGENTS.md`
- **Claude 轉址**：`CLAUDE.md`
- **使用說明與 CI**：`README.md`

> 若你需要查完整專案定義，請直接從本檔開始。
