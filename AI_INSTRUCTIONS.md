# AI 指示詞 — 瑜伽課表產生器

這份文件是本專案的**單一事實來源**。任何 AI 助理（Claude / ChatGPT / Copilot 等）
接手這個專案時，請先完整讀完本文件，再動任何一行程式。

規格是累積演化出來的，每一條都有原因。**不要憑直覺改動**，若要偏離請先問人。

---

## 0. 這個專案在做什麼

讀一份 CSV（一個月的瑜伽課表，每週固定循環），產生**單一個 HTML 檔**：

- 不需要伺服器，雙擊即可開
- 內含寬版（週課表格線）與窄版（單日輪播）兩種版型
- 中／英雙語即時切換
- 分類色票可點擊篩選
- 可一鍵匯出成 PNG 圖片

執行：

```bash
python3 build.py                      # 自動抓 data/ 下最新的 CSV
python3 build.py --csv data/xxx.csv --month 10 --branch 古亭館
```

---

## 1. 資料格式（CSV）

UTF-8 with BOM。欄位固定如下，順序不可變：

| 欄位 | 必填 | 內容 | 說明 |
|---|---|---|---|
| 星期 | ✔ | 一／二／三／四／五／六／日 | 也接受 1–7 |
| 開始時間 | ✔ | `07:00` | 24 小時制，補零 |
| 結束時間 | ✔ | `08:00` | |
| 課程名稱 | ✔ | `哈達瑜伽` | |
| 老師 | ✔ | `JAI（印籍）（EN）` | 括號註記見 §4 |
| 分類 | ✔ | 見 §3 | |
| 級別 | ✔ | 初／中／高 | |
| 異動類型 | | 空／`代課`／`暫停`／`代課／暫停` | |
| 代課老師 | | `Vivi` | 僅代課時填 |
| 異動日期 | | `9/8` 或 `9/7、9/28` | 多筆以「、」分隔 |
| 教室 | | `晴室` | **目前不顯示**，保留備用 |

課表每週循環，**每堂課只填一列**，不要展開整個月。

`代課／暫停` 的日期順序：**第一個日期是代課日，其餘是暫停日**。

---

## 2. 產出的三個檢查腳本（改完一定要跑）

```bash
python3 build.py
python3 checks/occl.py       # 遮蔽／重疊／裁切
python3 checks/wcag.py       # 對比度（寬版）
python3 checks/wcag.py 375   # 對比度（窄版）
python3 checks/catcheck.py   # 色票對應與堂數
FAKE_CLOCK=1 python3 checks/wcag.py   # 固定時間驗證「今天／已開始」
```

**通過標準：全部為 0。** 任何一項非 0 都是 bug，不是可接受的近似。

- `occl.py`：逐格檢查每個文字元素是否超出卡片、與其他元素重疊、或被裁切
- `wcag.py`：逐個文字節點計算實際疊算底色後的對比度，一般文字需 ≥ 4.5:1、大字 ≥ 3:1
- `catcheck.py`：比對每張卡片的顏色是否等於其分類色票，並核對堂數與 CSV 一致

`build.py` 本身也會印出對比度稽核摘要與資料警告（WARN）。

---

## 3. 分類與配色

11 個分類。顏色是用 **CIEDE2000 感知色差最佳化**算出來的，
在飽和度 ≤ 44%、明度 42–58% 的限制下讓最接近的兩色差距最大化。

最低色差 **ΔE00 = 18.6**（原本憑直覺配的版本只有 10.7，放鬆／阿斯坦加、
陰瑜伽／寰宇在 14px 小方塊上分不出來）。

| 分類 | 色票 | 卡片標籤 | 英文 |
|---|---|---|---|
| 伸展 | `#4792B8` | 伸展 | Stretch |
| 肌耐力/核心/快節奏 | `#B4747C` | 力 | Core |
| 放鬆 | `#47904C` | 放鬆 | Relax |
| 按摩 | `#B49F74` | 按摩 | Massage |
| 熱課程 | `#A76B44` | 熱 | Hot |
| 阿斯坦加 | `#9BBD56` | 阿斯 | Asht. |
| 瑜伽輪 | `#7E6FB8` | （不上標籤） | Yoga Wheel |
| 陰瑜伽 | `#42A99B` | （不上標籤） | Yin |
| 寰宇系列 | `#3C3C9A` | 寰宇 | Univ. |
| 付費課 | `#9A3C87` | 付費（另加 $350） | Paid |
| 姿勢正位 | `#6C82C0` | 正位 | Align |

**規則**

- 卡片標籤要短（1–2 字），因為並排時只有約 66px 寬
- `瑜伽輪`／`陰瑜伽` 不上分類標籤：課程名稱本身就等於分類名，標籤會重複；
  但**色票仍要列出**
- `高難度` 已併入 `肌耐力/核心/快節奏`（`CAT_FIX`），難度改由右上角標籤表達
- 分類找不到色票時 `build.py` 會**直接中斷**，不會靜默套灰色
- 顏色的三種用法：原色（左側色條）、混白 72%（分類標籤底）、混白 88%（卡片底）
- 卡片內的文字色不寫死，用 `fit()` 從分類色自動加深到對比 ≥ 4.6:1

---

## 4. 課程名稱與老師的正規化

在 `build.py` 開頭的對照表處理，**不要改原始 CSV**：

- `NAME_FIX`：修正課名（`基礎瑜伽 Fundamental` → `基礎瑜伽`、`豪宇` → `寰宇`、
  `慢流輪` → `慢流暢`、`墊上＿皮拉提斯` → `墊上皮拉提斯`）
- `KEEP_YOGA`：省略課名裡「瑜伽」二字的例外（`陰瑜伽`、`瑜伽輪`、`瑜伽提斯`），
  其餘一律省略（`哈達瑜伽` → `哈達`）
- `CAT_FIX`：修正分類
- `ALWAYS_EN`：印籍且英文授課的老師（目前 `JAI`、`Roushan`），
  CSV 漏標 `（EN）` 時自動補上
- `EN_TEACHER`：中文姓名的羅馬拼音
- `EN_NAME`：課名英譯

老師的 `（印籍）`／`（EN）` 會轉成 `印` / `EN` 小標籤（英文模式 `IN` / `EN`）。

`build.py` 會在產檔時警告：分類漏色票、簡化後同名、同一位老師標註不一致、
異動類型與代課老師欄矛盾。**這些警告要看，不要忽略。**

---

## 5. 卡片版面（四象限）

```
┌──────────────────────────────┐
│ 課名                  進階   │  右上：難度標籤
│ 09–10                        │
│ JAI 印 EN                    │  左上：課名／時間／老師
│                              │
│ 9/28 暫停              力    │  左下：異動標籤　右下：分類標籤
└──────────────────────────────┘
```

- 上區塊貼頂、下區塊 `margin-top:auto` 貼底，四角位置固定
- 左側 3px 分類色條（高難度類已取消加粗邊框，所有卡片邊框風格一致）
- 難度只標「中等」「進階」，初級不標（88/107 堂是初級，全標是雜訊）
  - 中等：綠底 `#e6ecdf` / `#4c6636`
  - 進階：橙底 `#f6e2d8` / `#a04a22`
  - 難度標籤是 flex 項目（與課名同列），**不要改回絕對定位**——
    絕對定位會讓標籤高度超過課名行高、壓到下一行的時間
- 異動一律用標籤，日期在前：`9/8 Vivi 代`（藍底）／`9/28 暫停`（紅底）
  - **暫停不改變卡片配色**：暫停通常只影響單日，整張卡轉灰會讓人誤以為整月停開
- 時間格式：起訖都在整點時兩端都省略分鐘（`11:00–12:00` → `11–12`），
  只有一端整點則完整顯示。非 60 分鐘的課在時間後加註分鐘數 `(75)`

### 自動縮排（fitCells）

並排卡片放不下時，逐級套用 `f1`→`f4` 縮小字級與行高，
`f3` 起隱藏難度與國籍標籤，`f4` 隱藏分類標籤與分鐘註記。
在字型載入完成後、語言切換後、匯出前都會重跑。

---

## 6. 寬版（週課表）

- 固定 2040px 寬。縱軸時間、橫軸星期
- **時間軸分段壓縮**：七天都沒課的空檔若超過 15 分鐘，壓成固定 20px
  並鋪斜紋帶表示被剪斷；不足 15 分鐘維持原比例。
  軸的起訖是第一堂課開始與最後一堂結束，不補到整點
- 同一天時間重疊的課並排（貪婪法配欄），未重疊的佔滿整欄
- 七天之間的垂直分隔線 2px 深色（`--lane`），水平格線 1px 淺色 —— 讓泳道感壓過時間感
- **表格沒有外框**，只有內部線條
- 今天：表頭底色加深 + 「今天」標籤 + 整欄淡金色薄膜
- 今天已開始的課：`filter: opacity(.42)` 淡化，每 60 秒重算
  （用 `filter` 而非 `opacity`，才能與篩選的 `opacity` 疊加而不互相覆蓋）

### Dock（常駐）

- 標題區（標題／館別／署名／功能列／分類色票）`position:sticky; top:0`
- 星期列 `sticky; top: var(--dockH)`，`--dockH` 由 JS 量測標題區實際高度
- 時間欄 `sticky; left:0` —— 橫向捲到週日還看得到刻度
- 功能列在寬版用 `position:fixed`（sticky 只鎖垂直，橫捲時按鈕會跑掉）
  - ⚠️ **但視窗 ≤ 900px 時必須改回 `static`**。紙張固定 2040px，手機用寬版時
    視窗遠比紙張窄，視窗右上的固定功能列會直接壓在標題與館別上面。
    媒體查詢裡把 `.trow` 改成縱向堆疊，功能列排到標題下方靠左；
    色票同時改成單行橫向捲動，避免折成五行把 dock 撐到 277px
    （實測 400px 寬時 dockH 從 277px 降到 193px）

---

## 7. 窄版（單日輪播）

一次一天，以當天為中心。七欄格線在 375px 下每欄只剩 50px，中文課名無法閱讀，
所以窄版**不是**把寬版縮小，而是另一套版面。

- 每日面板固定 320px，容器有多寬就排多少天（`floor((W − peek + gap) / step)`）
- 當前日**置中**：左右內距各 `(可視寬 − 320)/2`，兩端的日子也能捲到正中
- 前後日露出邊緣並淡出，淡出寬度 = 被切那天的可見寬度（限 18–130px）
- 左右懸浮鈕貼齊視窗左右緣（`fixed`，左 23px / 右 `innerWidth − 23`）
- **無縫循環**：前後各複製一整組面板（共 21 片），捲進複製區後靜默把
  `scrollLeft` 搬回中央那組。實測搬移前後畫面逐像素完全相同（ΔE = 0）
  —— **不要改回瞬間跳轉**
- 觸控用原生 scroll-snap；桌機另接 pointer 事件手動拖曳（原生橫捲不吃滑鼠拖曳）
- 七天全放得下時（≥ 2312px）自動關閉輪播互動：隱藏按鈕、關閉吸附與拖曳、
  隱藏淡出、七天水平居中、隱藏複製面板
- 星期 27px / weight 700，是全頁最大的字
- 捲過面板標頭後浮現 dock 條顯示當前日（輪播容器有 `overflow-x:auto`，
  瀏覽器會把 `overflow-y` 也算成 auto，導致 sticky 失效，所以不能直接對標頭用 sticky）
  - dock 的星期必須在**滑動過程中即時跟隨**，不能等吸附結束。
    軌道的 `scroll` 事件用 rAF 節流直接算 `round(scrollLeft / step)` 換標題；
    吸附結束後的 110ms debounce 只負責 `normalize()` 與 `markCur()`。
    只靠 debounce 的話，手機慣性滑動期間星期會停在舊的那天，
    而且慣性可以持續很久，那段空窗非常明顯
  - 「今天」標籤只在該片面板真的是今天時顯示（`istoday`），
    複製過去的標籤預設隱藏

### 跨日時間對齊

窄版用**共用小時列**：左側 48px 時間欄（整點刻度 + 早／午／晚圖示），
每一天都有同樣 12 個小時列，該小時沒課就留空。

列高由 JS 取七天之中最高者同步（`syncRows`），時間欄的對應格套用同一高度。

**這件事很重要**：不對齊的話每天都從第一張卡片開始堆疊，
週二第一堂是 10:00、週三是 09:00 的差異完全看不出來，學員會以為每天同時開始。

---

## 8. 互動

- **語言切換**：中英文都寫進 DOM（`<span class="zh">`／`<span class="en">`），
  切換只改 `.sheet` 上的 class。
  ⚠️ **陷阱**：任何針對標籤內層 span 的 CSS 都要用直接子選擇器（`.x > span`），
  否則會蓋掉語言切換的 `display:none`，導致中英夾雜。這個坑踩過三次。
  另外整組內容必須在 `#sheet` 之內，否則 `.sheet .en{display:none}` 不生效
- **版型切換**：寫入 `localStorage['yoga-view']`；沒存過時依螢幕寬度
  （< 900px 用窄版）。判斷腳本放在 `<body>` 開頭、內容解析前執行，避免閃動
- **分類篩選**：單選。點 A 篩選 A，再點 A 顯示全部，點 B 直接換成 B。
  未命中的卡片降到 14% 不透明度（用淡化而非隱藏，否則時間軸出現空洞，
  看不出該時段其他課排得多滿）

---

## 9. 匯出 PNG

html2canvas，2 倍解析度。**匯出樣式跟隨當前模式**（寬／窄 × 中／英），檔名也跟著變：

| 模式 | 檔名 |
|---|---|
| 寬版 中文 | `<館別>-<月>月課表.png` |
| 寬版 英文 | `<Branch>-<Month>-schedule.png` |
| 窄版 中文 | `<館別>-<月>月課表-直式.png` |
| 窄版 英文 | `<Branch>-<Month>-schedule-vertical.png` |

匯出時暫時套用這些 class，完成後還原：

| class | 作用 |
|---|---|
| `flat` | 取消 sticky / fixed（html2canvas 對這兩者定位會錯亂）。⚠️ 時間欄要設 `position:relative` 而非 `static`，否則裡面絕對定位的刻度會跑到紙張右上角 |
| `noto` | 關閉今天的欄位底色、「今天」標籤、日期底線加深 |
| `nopast` | 關閉已過時段的淡化（時間性狀態存成圖片會過期而誤導） |
| `weekexp` | 窄版才加：軌道 `overflow:visible`、內距歸零、寬度 `max-content`、隱藏複製面板 → 一次輸出完整七天，不循環重複 |

⚠️ **順序**：這些 class 要在讀 `sheet.offsetWidth` **之前**加上，
否則 html2canvas 拿到的還是未展開的寬度。

匯出時隱藏（`data-noexport`）：功能列、左右懸浮鈕、「顯示全部」按鈕、dock 條。
**保留**：分類色票。

---

## 10. 無障礙

全站通過 **WCAG AA**（一般文字 4.5:1、大字 3:1），中英文各 0 個不合格節點。

- 灰色階：`--ink` `#33302b`／`--ink2` `#635d55`／`--ink3` `#6e675d`
- 標籤底色用固定值，**不要用半透明黑**（`rgba(0,0,0,.055)`）——
  半透明會隨底下卡片顏色變動，對比度不可控
- 分類標籤的文字色用 `fit()` 自動加深，換配色不用手調
- 刻意降低視覺權重的內容（已過時段的淡化、被篩選淡化的卡片）比照 disabled，
  不計入對比度要求

---

## 11. 樣式基調

- 米白紙張 `#F4F0E9`，圓角（紙張無框、表格 10–12px、卡片 7–8px、按鈕膠囊）
- 早／午／晚用紙色逐段加深表示（`--band1/2/3`），不畫標題列
- 圖示全部是內嵌 SVG 線稿，**不用 emoji、不依賴圖示字型**
  （日出／太陽／新月、方格／條列、下載箭頭、左右箭頭）
- 字型 Noto Sans TC（300/400/500/700），fallback PingFang TC → 微軟正黑體
- 列印樣式：A2 橫式

---

## 12. 待確認 / 已知取捨

- **週六 12:15 溫和緩（Maggie）**：CSV 異動類型填「暫停」但代課老師欄填了 `Elan`，
  目前依「暫停」處理並忽略 `Elan`。需向館方確認
- **簡化後同名**：`伸展`（伸展瑜伽／瑜伽伸展）、`基礎`（基礎瑜伽／瑜伽基礎）、
  `阿斯坦加`（阿斯坦加／阿斯坦加瑜伽）。時段與老師不同故表上不會撞在一起，
  但若課程內容有差異需另取名
- **火箭入門**：級別欄是「中」，所以標「中等」。若實際屬進階，改 CSV 級別為「高」
- **Ashish**：11 堂全部只標`（印籍）`未標`（EN）`。若印籍一律英文授課，
  規則應改成「有 印 就自動加 EN」，白名單就不用維護
- **卡片底色差異很小**（混白 88%，ΔE ≈ 2）。這是刻意的，一整片彩色卡片會很吵；
  分類辨識靠左側色條、右下標籤、底部色票

---

## 13. 改動流程（給 AI 的硬性要求）

1. 讀完本文件
2. 改 `build.py`（不要改產出的 HTML；HTML 是產物，改了會被下次覆蓋）
3. `python3 build.py` — 看 WARN
4. 跑完 §2 的四項檢查，全部必須是 0
5. 用 Playwright 截圖目視確認（寬版 2100px、窄版 375px，中英各一）
6. 把新規格補進本文件，包含**為什麼**這樣做

不要為了讓檢查通過而放寬檢查標準。檢查失敗代表版面真的有問題。

---

## 14. 無障礙（WCAG 2.2 AA）

不只對比度。四個原則都處理過，改動時不要破壞。

### 可感知 Perceivable

- **1.1.1**：所有裝飾性 SVG 都有 `aria-hidden="true" focusable="false"`
- **1.3.1**：每張課程卡是 `role="group"`，`aria-label` 完整敘述
  「週一 壁繩，09:00 至 10:00，JAI（印籍），英文授課，肌耐力/核心/快節奏，初級，9/28 暫停」。
  視覺版面是空間性的（絕對定位），沒有這個標籤螢幕閱讀器讀不出對應關係。
  中英兩份都預先產好（`aria-label` / `data-lab-en`），切語言時互換
- **1.4.1 不可只靠顏色**：
  - 今天 → 底色 + 「今天」文字標籤
  - 已開始 → 灰階 + 「已開始」文字標籤（`markPast` 動態插入 `.done`）
  - 暫停／代課 → 標籤文字，不只顏色
- **1.4.3 對比度**：一般文字 ≥ 4.5:1、大字 ≥ 3:1，中英各 0 個不合格
  - ⚠️ **已開始的課不能用 opacity 淡化**。任何降低不透明度的做法都會讓文字
    對比掉到 4.5 以下（最弱的標籤本來就只有 4.65）。所以改成整張卡轉灰階
    （`#eceae5` 底、`#46423c` 課名），這組配色早先已驗證過對比
  - `checks/wcag.py` 會累乘祖先的 `opacity` 與 `filter:opacity()` 後再計算，
    所以想用淡化蒙混會被抓出來
- **1.4.10 Reflow**：寬版固定 2040px 需橫捲；小於 900px 自動切窄版，
  窄版可在 375px 正常閱讀

### 可操作 Operable

- **2.1.1 鍵盤**：所有互動都是原生 `<button>`，Tab 可達、Enter/Space 可觸發
- **2.4.7 焦點可見**：統一的 `:focus-visible` 3px 外框（`#2f5d86`，offset 2px）
- **2.5.8 目標尺寸**：色票按鈕最小高度 26px（≥ 24×24），
  切換鈕 30px、下載鈕 34–42px、左右懸浮鈕 38px
- **2.3.3 動態**：`prefers-reduced-motion: reduce` 時關閉平滑捲動、
  轉場與載入轉圈

### 可理解 Understandable

- **3.1.1／3.1.2 語言**：`<html lang>` 隨語言切換更新；
  雙語 span 各自標 `lang="zh-Hant"` / `lang="en"`，
  所以螢幕閱讀器會用正確的語音引擎唸

### 穩健 Robust

- **4.1.2 名稱／角色／狀態**：
  - 版型與語言切換是 `role="group"` + 每個按鈕 `aria-pressed`
  - 色票按鈕 `aria-pressed`
  - 下載中 `aria-busy="true"`
  - `#sheet` 是 `<main>` 地標
- **4.1.3 狀態訊息**：篩選結果用 `aria-live="polite"` 朗讀
  「已篩選：熱，共 12 堂」／「顯示全部課程」
- **循環用的複製面板全部 `aria-hidden="true"`**，
  否則螢幕閱讀器會把整週唸三遍

### 檢查

```bash
python3 checks/wcag.py         # 寬版
python3 checks/wcag.py 375     # 窄版
FAKE_CLOCK=1 python3 checks/wcag.py    # 以週三 14:20 驗證「今天／已開始」狀態
```

`FAKE_CLOCK=1` 會把瀏覽器時間固定在 2026-09-02 14:20（+08:00），
三支檢查都支援。時間相關的狀態（今天欄位、已開始淡化）平常測不到，
一定要用這個模式驗證。
