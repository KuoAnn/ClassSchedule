# AI 指示詞 — 兼容層

這份檔案已退役，不再作為主要規格來源。

本專案的實際規格已移至 **[AGENTS.md](AGENTS.md)**，
Claude/ChatGPT/Copilot 等 AI 助理請先讀 **[AGENTS.md](AGENTS.md)**，再動任何一行程式。

> 這個檔案保留的目的只有兼容既有連結與歷史參照，不再累積專案規格。

若你在本專案內看到舊連結指向本檔，請改成指向 [AGENTS.md](AGENTS.md)。


## 7. 窄版（單日輪播）

一次一天，以當天為中心。七欄格線在 375px 下每欄只剩 50px，中文課名無法閱讀，
所以窄版**不是**把寬版縮小，而是另一套版面。

- 每日面板固定 320px，容器有多寬就排多少天（`floor((W − peek + gap) / step)`）
- 當前日**置中**：左右內距各 `(可視寬 − 320)/2`，兩端的日子也能捲到正中
- 前後日露出邊緣並淡出，淡出寬度 = 被切那天的可見寬度（限 18–130px）
- 左右懸浮鈕貼齊視窗左右緣（`fixed`，左 23px / 右 `innerWidth − 23`）
- **無縫循環**：前後各複製一整組面板（共 21 片），捲進複製區後靜默把
  `scrollLeft` 搬回中央那組。實測搬移前後畫面逐像素完全相同（ΔE = 0）
  —— **不要改回瞬間跳轉**
- 觸控用原生 scroll-snap；桌機另接 pointer 事件手動拖曳（原生橫捲不吃滑鼠拖曳）
- 七天全放得下時（≥ 2312px）自動關閉輪播互動：隱藏按鈕、關閉吸附與拖曳、
  隱藏淡出、七天水平居中、隱藏複製面板
- 星期 27px / weight 700，是全頁最大的字
- **星期標頭往下捲時要固定在上方，但不能看起來像另外浮出一條 bar**。
  輪播容器有 `overflow-x:auto`，瀏覽器會把 `overflow-y` 也算成 auto，
  導致直接對面板內的標頭用 `position:sticky` 無效（參考容器變成軌道本身）。
  所以另做一個 `.ndock` 由 JS 控制顯示，但**樣式與面板內的標頭共用同一組規則**
  （`.dhd,.ndock{...}`：27px/700、置中、2px 底線、同樣的內距與底色），
  釘住時看起來就是同一個標頭停在原位。實測兩者字級、字重、高度、
  文字中心 x、底線顏色完全一致
  - 底線寬度也要一致：`.ndock` 本身不畫底線，改用 `.dday::after` 畫一條
    寬度等於 `--dayw` 的線並置中，否則會是整個視窗寬的長線而非單日寬
    （因此 `--dayw` 等變數要放在 `:root`，不能只放在 `.ntrack`）
  - 面板標頭與 dock 的內容結構必須相同（`<span class="dday">星期<i class="tdy">今天</i></span>`），
    這樣 `setDockDay` 只要直接複製 innerHTML
  - ⚠️ **交接時機要用「面板標頭的下緣」而不是容器上緣**。
    若用容器上緣，標頭剛滑到固定列下方時 dock 就出現，
    但真正的標頭還完整露在下面 → 畫面同時出現兩個星期。
    正確條件是 `hd.getBoundingClientRect().bottom <= dockH`，
    另外加 `.ntrack.pinned .dhd{visibility:hidden}` 當保險
  - ⚠️ 共用規則 `.dhd,.ndock{display:flex}` 寫在 `.ndock{display:none}` 後面，
    會把隱藏狀態蓋掉導致 dock 永遠顯示。必須用 `.ndock:not(.show){display:none}`
    這種更強的選擇器補回
  - dock 的星期必須在**滑動過程中即時跟隨**，不能等吸附結束。
    軌道的 `scroll` 事件用 rAF 節流直接算 `round(scrollLeft / step)` 換標題；
    吸附結束後的 110ms debounce 只負責 `normalize()` 與 `markCur()`。
    只靠 debounce 的話，手機慣性滑動期間星期會停在舊的那天，
    而且慣性可以持續很久，那段空窗非常明顯
  - 「今天」標籤只在該片面板真的是今天時顯示（`istoday`），
    複製過去的標籤預設隱藏
  - dock 的星期**置中**，且要對齊當前面板的中心（因此 dock 要補
    `padding-left` 等於時間欄寬度、`margin-right` 抵銷 `.nlist` 的負邊距）
  - ⚠️ 「今天」標籤不能留在 flex 排版流裡，否則星期會被推離中心 30px。
    也**不能只加 `position:absolute`** —— flex 容器的絕對定位子元素會依
    `justify-content` 定位，結果直接疊在星期上面。
    正解是把星期包一層 `.dday{position:relative}`，
    標籤放進去用 `left:100%` 掛在右側

### 跨日時間對齊

窄版用**共用小時列**：左側 48px 時間欄（整點刻度 + 早／午／晚圖示），
每一天都有同樣 12 個小時列，該小時沒課就留空。

列高由 JS 取七天之中最高者同步（`syncRows`），時間欄的對應格套用同一高度。

**這件事很重要**：不對齊的話每天都從第一張卡片開始堆疊，
週二第一堂是 10:00、週三是 09:00 的差異完全看不出來，學員會以為每天同時開始。

---

## 8. 互動

- **語言切換**：中英文都寫進 DOM（`<span class="zh">`／`<span class="en">`），
  切換只改 `.sheet` 上的 class。
  ⚠️ **陷阱**：任何針對標籤內層 span 的 CSS 都要用直接子選擇器（`.x > span`），
  否則會蓋掉語言切換的 `display:none`，導致中英夾雜。這個坑踩過三次。
  另外整組內容必須在 `#sheet` 之內，否則 `.sheet .en{display:none}` 不生效
- **版型切換**：寫入 `localStorage['yoga-view']`；沒存過時依螢幕寬度
  （< 900px 用窄版）。判斷腳本放在 `<body>` 開頭、內容解析前執行，避免閃動
- **分類篩選**：單選。點 A 篩選 A，再點 A 顯示全部，點 B 直接換成 B。
  未命中的卡片降到 14% 不透明度（用淡化而非隱藏，否則時間軸出現空洞，
  看不出該時段其他課排得多滿）

---

## 9. 匯出 PNG

html2canvas，2 倍解析度。**匯出樣式跟隨當前模式**（寬／窄 × 中／英），檔名也跟著變：

| 模式 | 檔名 |
|---|---|
| 寬版 中文 | `<館別>-<月>月課表.png` |
| 寬版 英文 | `<Branch>-<Month>-schedule.png` |
| 窄版 中文 | `<館別>-<月>月課表-直式.png` |
| 窄版 英文 | `<Branch>-<Month>-schedule-vertical.png` |

匯出時暫時套用這些 class，完成後還原：

| class | 作用 |
|---|---|
| `flat` | 取消 sticky / fixed（html2canvas 對這兩者定位會錯亂）。⚠️ 時間欄要設 `position:relative` 而非 `static`，否則裡面絕對定位的刻度會跑到紙張右上角 |
| `noto` | 關閉今天的欄位底色、「今天」標籤、日期底線加深 |
| `nopast` | 關閉已過時段的淡化（時間性狀態存成圖片會過期而誤導） |
| `weekexp` | 窄版才加：軌道 `overflow:visible`、內距歸零、寬度 `max-content`、隱藏複製面板 → 一次輸出完整七天，不循環重複 |

⚠️ **順序**：這些 class 要在讀 `sheet.offsetWidth` **之前**加上，
否則 html2canvas 拿到的還是未展開的寬度。

匯出時隱藏（`data-noexport`）：功能列、左右懸浮鈕、「顯示全部」按鈕、dock 條。
**保留**：分類色票。

---

## 10. 無障礙

全站通過 **WCAG AA**（一般文字 4.5:1、大字 3:1），中英文各 0 個不合格節點。

- 灰色階：`--ink` `#33302b`／`--ink2` `#635d55`／`--ink3` `#6e675d`
- 標籤底色用固定值，**不要用半透明黑**（`rgba(0,0,0,.055)`）——
  半透明會隨底下卡片顏色變動，對比度不可控
- 分類標籤的文字色用 `fit()` 自動加深，換配色不用手調
- 刻意降低視覺權重的內容（已過時段的淡化、被篩選淡化的卡片）比照 disabled，
  不計入對比度要求

---

## 11. 樣式基調

- 米白紙張 `#F4F0E9`，圓角（紙張無框、表格 10–12px、卡片 7–8px、按鈕膠囊）
- 早／午／晚用紙色逐段加深表示（`--band1/2/3`），不畫標題列
- 圖示全部是內嵌 SVG 線稿，**不用 emoji、不依賴圖示字型**
  （日出／太陽／新月、方格／條列、下載箭頭、左右箭頭）
- 字型 Noto Sans TC（300/400/500/700），fallback PingFang TC → 微軟正黑體
- 列印樣式：A2 橫式

---

## 12. 待確認 / 已知取捨

- **週六 12:15 溫和緩（Maggie）**：CSV 異動類型填「暫停」但代課老師欄填了 `Elan`，
  目前依「暫停」處理並忽略 `Elan`。需向館方確認
- **簡化後同名**：`伸展`（伸展瑜伽／瑜伽伸展）、`基礎`（基礎瑜伽／瑜伽基礎）、
  `阿斯坦加`（阿斯坦加／阿斯坦加瑜伽）。時段與老師不同故表上不會撞在一起，
  但若課程內容有差異需另取名
- **火箭入門**：級別欄是「中」，所以標「中等」。若實際屬進階，改 CSV 級別為「高」
- **Ashish**：11 堂全部只標`（印籍）`未標`（EN）`。若印籍一律英文授課，
  規則應改成「有 印 就自動加 EN」，白名單就不用維護
- **卡片底色差異很小**（混白 88%，ΔE ≈ 2）。這是刻意的，一整片彩色卡片會很吵；
  分類辨識靠左側色條、右下標籤、底部色票

---

## 13. 改動流程（給 AI 的硬性要求）

1. 讀完本文件
2. 改 `build.py`（不要改產出的 HTML；HTML 是產物，改了會被下次覆蓋）
3. `python3 build.py` — 看 WARN
4. 跑完 §2 的四項檢查，全部必須是 0
5. 用 Playwright 截圖目視確認（寬版 2100px、窄版 375px，中英各一）
6. 把新規格補進本文件，包含**為什麼**這樣做

不要為了讓檢查通過而放寬檢查標準。檢查失敗代表版面真的有問題。

---

## 14. 無障礙（WCAG 2.2 AA）

不只對比度。四個原則都處理過，改動時不要破壞。

### 可感知 Perceivable

- **1.1.1**：所有裝飾性 SVG 都有 `aria-hidden="true" focusable="false"`
- **1.3.1**：每張課程卡是 `role="group"`，`aria-label` 完整敘述
  「週一 壁繩，09:00 至 10:00，JAI（印籍），英文授課，肌耐力/核心/快節奏，初級，9/28 暫停」。
  視覺版面是空間性的（絕對定位），沒有這個標籤螢幕閱讀器讀不出對應關係。
  中英兩份都預先產好（`aria-label` / `data-lab-en`），切語言時互換
- **1.4.1 不可只靠顏色**：
  - 今天 → 底色 + 「今天」文字標籤
  - 已結束 → 淡化 + 「已結束」文字標籤；進行中 → 綠色外圈 + 「進行中」文字標籤
    （`markPast` 動態插入 `.stt`，兩者都不是只靠顏色）
  - 暫停／代課 → 標籤文字，不只顏色
- **1.4.3 對比度**：一般文字 ≥ 4.5:1、大字 ≥ 3:1，中英各 0 個不合格
  - **例外：已結束的課刻意用 `opacity` 淡化，此處明確不套對比要求**。
    這是產品決定——改成灰階後「已結束」的區分度不夠明顯，
    而已結束的課本來就不需要被閱讀。`checks/wcag.py` 會把
    `.st-done` 底下的節點排除，並在輸出印出豁免的節點數，
    所以豁免範圍是可見、可審核的，不會被拿來蒙混其他地方
  - `checks/wcag.py` 仍會累乘祖先的 `opacity` 與 `filter:opacity()` 後再計算，
    所以在 `.st-done` 以外的地方想用淡化蒙混還是會被抓出來
  - 進行中／待開課的卡片維持完整對比
- **1.4.10 Reflow**：寬版固定 2040px 需橫捲；小於 900px 自動切窄版，
  窄版可在 375px 正常閱讀

### 可操作 Operable

- **2.1.1 鍵盤**：所有互動都是原生 `<button>`，Tab 可達、Enter/Space 可觸發
- **2.4.7 焦點可見**：統一的 `:focus-visible` 3px 外框（`#2f5d86`，offset 2px）
- **2.5.8 目標尺寸**：色票按鈕最小高度 26px（≥ 24×24），
  切換鈕 30px、下載鈕 34–42px、左右懸浮鈕 38px
- **2.3.3 動態**：`prefers-reduced-motion: reduce` 時關閉平滑捲動、
  轉場與載入轉圈

### 可理解 Understandable

- **3.1.1／3.1.2 語言**：`<html lang>` 隨語言切換更新；
  雙語 span 各自標 `lang="zh-Hant"` / `lang="en"`，
  所以螢幕閱讀器會用正確的語音引擎唸

### 穩健 Robust

- **4.1.2 名稱／角色／狀態**：
  - 版型與語言切換是 `role="group"` + 每個按鈕 `aria-pressed`
  - 色票按鈕 `aria-pressed`
  - 下載中 `aria-busy="true"`
  - `#sheet` 是 `<main>` 地標
- **4.1.3 狀態訊息**：篩選結果用 `aria-live="polite"` 朗讀
  「已篩選：熱，共 12 堂」／「顯示全部課程」
- **循環用的複製面板全部 `aria-hidden="true"`**，
  否則螢幕閱讀器會把整週唸三遍

### 檢查

```bash
python3 checks/wcag.py         # 寬版
python3 checks/wcag.py 375     # 窄版
FAKE_CLOCK=1 python3 checks/wcag.py    # 以週三 14:20 驗證「今天／已開始」狀態
```

`FAKE_CLOCK=1` 會把瀏覽器時間固定在 2026-09-02 14:20（+08:00），
三支檢查都支援。時間相關的狀態（今天欄位、已開始淡化）平常測不到，
一定要用這個模式驗證。
